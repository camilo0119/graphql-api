type Query {
  hello (name: String): String!,
  quantity: Int!,
  user(id: ID): [User!]!
}

type User {
  id: ID!,
  name: String!,
  lastName: String!
}

type Author {
  id: ID!,
  name: String!,
  country: String!,
  register_by: User!
}